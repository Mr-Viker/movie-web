.container.big-title
    .row
        header.page-header
            h1 #{title}
            small 重度科幻迷

nav.navbar.navbar-default.navbar-fixed-top
    .container
        .navbar-header
            a.navbar-brand(href="/") 电影天堂
        if user
            p.navbar-text.navbar-right
                span 欢迎你, #{user.username}&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
                a.navbar-link(href="/user/logout") 退出登录
        else
            p.navbar-text.navbar-right
                a.navbar-link(href="/user/signup") 注册
                span &nbsp;|&nbsp;
                a.navbar-link(href="/user/signin") 登录
